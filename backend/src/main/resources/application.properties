spring.application.name=jee-college-predictor
server.port=8080

# FIX #4: Expose actuator health endpoint (used by frontend health() check in api.js)
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# FIX #5: Allow all origins via config (CORS also handled in CorsConfig.java)
# Logging level for debugging CSV load issues
logging.level.com.jee=DEBUG
spring.data.mongodb.uri=mongodb+srv://LikhithaSai:Lali182k5@cluster0.9chkf.mongodb.net/jee?retryWrites=true&w=majority